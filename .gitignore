# Python bytecode / caches
__pycache__/
*.py[cod]
*$py.class

# (opcional) ambientes locais
.venv/
venv/
.env
.env.*

# CSVs de teste/importação (scripts/)
scripts/*.csv

# Nova versão do app (FastAPI + React) — vive num repositório à parte
# (cemec-portal); este repo é só o Streamlit legado.
server/
web/

# Configurações locais do Claude Code
.claude/